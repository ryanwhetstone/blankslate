<?php

/**
 * Example: Get child pages as array
 *
 * function _usdp_get_page_children($parent_id) {
 *   $page_children = get_pages(array(
 *     'child_of' => $parent_id,
 *     'sort_column' => 'menu_order'
 *   ));
 *
 *   return $page_children;
 * }
 */